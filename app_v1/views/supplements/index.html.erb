<h1>Listing supplements</h1>

<table>
  <thead>
    <tr>
      <th>Award</th>
      <th>Name</th>
      <th>Unit cost</th>
      <th>Quantity</th>
      <th>Total cost</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @supplements.each do |supplement| %>
      <tr>
        <td><%= supplement.award_id %></td>
        <td><%= supplement.name %></td>
        <td><%= supplement.unit_cost %></td>
        <td><%= supplement.quantity %></td>
        <td><%= supplement.total_cost %></td>
        <td><%= link_to 'Show', supplement %></td>
        <td><%= link_to 'Edit', edit_supplement_path(supplement) %></td>
        <td><%= link_to 'Destroy', supplement, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Supplement', new_supplement_path %>
